# 第五阶段测试（主持人初始化设定）

## 目标
验证主持人配置相关接口的幸福路径：API 配置、规则、玩家配置、验证、完成。

## 快速开始（PowerShell）
```powershell
cd "重新打造/各阶段测试用/5阶段测试"
.\test-phase5.ps1
```

## 覆盖范围
1. 健康检查 `/health`
2. 登录/注册测试账号 `testuser_phase5`
3. 创建测试房间
4. 更新 API 配置（provider/endpoint/headers/bodyTemplate）
5. 更新规则
6. 更新玩家/主体配置（人类/AI数量、时限、超时策略）
7. 验证状态标记
8. 完成配置并读取快照

## 期望输出
```
========================================
🎉 第五阶段测试通过
========================================
```

如有失败，脚本会抛出具体步骤的错误信息，可结合后端日志排查。

